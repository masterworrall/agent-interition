services:
  solid-agent-pods:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - pod-data:/data
    environment:
      SAP_PORT: 3000
      SAP_DATA_DIR: /data
      BOOTSTRAP_AGENTS: ${BOOTSTRAP_AGENTS:-}

volumes:
  pod-data:
